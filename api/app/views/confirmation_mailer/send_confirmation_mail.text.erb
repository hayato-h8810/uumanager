<% require "./app/components/views.rb"%>
<%= @email %>様

UUManagerにご登録いただき、ありがとうございます。このメールは、ご登録いただいたメールアドレスが正しいか確認するためのメールです。

下記のリンクをクリックしていただくと、メール認証が完了します。

<%= link_to "#{create_string_depend_on_the_environments()}/confirmation/#{@user.confirmation_sent_at.to_i}-#{@email}", "#{create_string_depend_on_the_environments()}/confirmation/#{@user.confirmation_sent_at.to_i}-#{@email}" %>

このURLの有効期限は<%= Time.at(@user.confirmation_sent_at.to_i + 3600).strftime("%y/%m/%d %T") %>です。このメールに心当たりがない方は、お手数ですがこのメールは破棄してください。
